# Release Index

Public release bundles comprise code, configuration, manifests, benchmark tables, audio products, and provenance records.

The release assembly helpers in `src/echorm/reports/` define the structured inventory from which public release indexes are derived.
